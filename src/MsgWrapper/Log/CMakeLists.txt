# generated automatically by messageGenerator on Dec  8 2022  22:00:32
# Do not Edit!
# use it by add_subdirectory()
set(WRAPPER_NAME wrapperLog)
add_library(${WRAPPER_NAME} STATIC Log.cpp)
target_link_libraries(${WRAPPER_NAME} ${catkin_LIBRARIES})
